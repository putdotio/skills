interface:
  display_name: "put.io Frontend Packages"
  short_description: "Standardize package repo delivery"
  default_prompt: "Use $putio-frontend-packages to structure this package repository around one repo-local verify command, CI guardrails, and an automatic release flow that only runs after verify passes."
