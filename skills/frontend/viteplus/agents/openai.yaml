interface:
  display_name: "put.io Frontend VitePlus"
  short_description: "Align repos with stock VitePlus"
  default_prompt: "Use $putio-frontend-viteplus to migrate or align this frontend repository with the stock VitePlus workflow across scripts, CI, tests, and packaging while preserving necessary repo-specific release logic."
